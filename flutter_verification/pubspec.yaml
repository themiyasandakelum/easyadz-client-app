name: verification_service
description: Identity verification - upload ID + selfie for manual admin review.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image_picker: ^1.0.7
  supabase_flutter: ^2.0.0
  path_provider: ^2.1.2
  path: ^1.8.3
